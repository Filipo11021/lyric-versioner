<div class="devise-layout">
  <h2 class="devise-heading">Resend unlock instructions</h2>

  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "devise-form" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div>
      <%= f.label :email, class: "devise-label" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "devise-input" %>
    </div>

    <div>
      <%= f.submit "Resend unlock instructions", class: "devise-button devise-button-primary" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
